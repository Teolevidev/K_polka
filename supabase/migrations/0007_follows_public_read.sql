-- ============================================================
--  Книжная полка — миграция 0007
--  Подписки публично читаются (для счётчиков и публичных профилей).
--  Писать может только сам подписчик за себя (политика follows manage own
--  из миграции 0001 остаётся в силе).
-- ============================================================

drop policy if exists "follows select" on follows;
create policy "follows select" on follows for select using (true);
